// Array, lista o arreglo, se colocan en indices y comienza en el numero 0
let seriesFavoritas = ['Friends', 'House', 'Breaking Bad', 'Los Picaprieda'];
console.log(seriesFavoritas); // Friends

// Un elemento del array
let series = ['Friends', 'House', 'Breaking Bad', 'Los Picaprieda'];
console.log(series[0]); // Friends

// Arrar dentro de un array
let costos = [0, 1, 2, ['otro arrary', 'diferentes variables', true]];
console.log(costos[3][0]); // otro array
